# Server Configuration
# Backend API runs on port 8000 (hardcoded in server.js). PORT here is for reference only.
NODE_ENV=development

# Testing: non-expiring access token for API tests (dev only; ignored when NODE_ENV=production).
# Use header: Authorization: Bearer test-token-no-expire (or your value if you set TEST_ACCESS_TOKEN).
TEST_ACCESS_TOKEN=test-token-no-expire

# Database Configuration
DB_HOST=srv1149167.hstgr.cloud
DB_PORT=3306
DB_NAME=u5gdchot-vizidot
DB_USER=api_vizidot_user
DB_PASSWORD="RPtze2.HTPHvT*E$#%#3"

# Firebase Configuration
FIREBASE_PROJECT_ID=vizidot-4b492
FIREBASE_DATABASE_URL=https://vizidot-4b492.firebaseio.com
# Provide the minified JSON blob (or base64-encoded JSON) for Firebase credentials.
# Leave empty for local dev: server will start and /health will work; auth/Firestore routes will fail.
FIREBASE_SERVICE_ACCOUNT_JSON=""

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Redis Configuration (for sessions)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# File Upload Configuration
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration (comma-separated list or *)
CORS_ORIGINS=http://localhost:3000
